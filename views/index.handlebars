<ul class="list-group">
  {{#each records}}
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <div class="p-3 flex-grow-1 d-flex justify-content-between align-items-center">
      <div>
        {{#switch this.category}}
            {{#case '交通出行'}} <i class="align-middle material-icons">traffic</i> {{/case}}
            {{#case '家居物業'}} <i class="align-middle material-icons">house</i> {{/case}}
            {{#case '休閒娛樂'}} <i class="align-middle material-icons">beach_access</i> {{/case}}
            {{#case '餐飲食品'}} <i class="align-middle material-icons">fastfood</i> {{/case}}
            {{#case '其他'}} <i class="align-middle material-icons">build</i> {{/case}}
        {{/switch}}
        <span class="align-middle">{{this.name}}</span>
        <small class="form-text text-muted">
          {{#formatDate this.date 'YYYY/MM/DD'}}{{/formatDate}}
        </small>
      </div>
      <div>
        $ {{this.amount}}
      </div>
    </div>
    <div>
      <a href="/records/{{this._id}}/edit" class="btn btn-light">修改</a>
      <form action="/records/{{{this._id}}}?_method=DELETE" method="POST" style="display: inline;">
        <button type="submit" class="btn btn-danger">刪除</button>
      </form>
    </div>
  </li>
  {{/each}}
</ul>
<div class="d-flex justify-content-center">
  <a href="/records/new" class="btn btn-light">新增支出</a>
</div>
